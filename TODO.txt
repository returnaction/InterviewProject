1) В бд может заменить что бы указывались номера вместа текста у User role

2) добавить общий обработчик ошибок

3) (например, common-lib) и подключить как зависимость  наши модели.

4) 🔐 Дополнительно (по желанию):
Если в будущем решишь использовать несколько ролей, можно:
Сохранять в токен List<String> вместо строки.
Вытаскивать с get("roles", List.class) и пробрасывать как "X-User-Roles" в заголовке через фильтр.

5) server.port: 8082 (на всех сервисах) — ок, можно менять при отладке вручную (или поставить 0 для масштабирования)